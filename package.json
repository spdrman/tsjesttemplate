{
  "name": "stock-symbols-api",
  "version": "1.0.0",
  "description": "Grabs list of symbols from data-provider",
  "main": "app.ts",
  "scripts": {
    "start": "ts-node src/app.ts",
    "prestart": "npm run build",
    "build": "tsc",
    "test": "jest",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "author": "fnchart",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "gts": "^3.1.1",
    "jest": "^29.4.1",
    "ts-jest": "^29.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5",
    "@types/node": "^14.11.2"
  },
  "dependencies": {
    "axios": "^1.3.2",
    "dotenv": "^16.0.3"
  }
}
